app:
  name: invalid_protocol_type

actors:
  - name: lifecycle_gateway
    script: tests/lua_api/scripts/lifecycle_service.lua
    instances: 1
    required: true
    network:
      tcp: "127.0.0.1:65534"
      protocol:
        envelope:
          type: 123

database:
  enabled: false

redis:
  enabled: false
